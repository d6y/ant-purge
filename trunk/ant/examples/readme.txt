The setup.sh script removes anything in output and logs 
subfolders and then creates some example files in
those folder and runs ant using the supplied
build.xml file.

Make sure you've copied purge.jar into
your Ant's lib/ directory.

